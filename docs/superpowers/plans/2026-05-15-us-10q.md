# 美股 10-Q 季报补齐 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** SEC EDGAR 支持 10-Q 季报下载，CLI 放开美股报告抓取

**Architecture:** EDGAR Provider 从 10-K 专用接口重构为通用 filings 接口（form_types 参数），Gateway 美股市分支透传参数，CLI 去 A 股硬限制

**Tech Stack:** Python 3.12+, requests, Click, Rich

---

### Task 1: EDGAR Provider — 10-K 专用 → 通用 filings 接口

**Files:**
- Modify: `src/data/providers/edgar.py`

- [ ] **Step 1: 添加 form type 映射常量，重构 `list_10k` → `list_filings`**

```python
# 在 SECEDGARProvider 类内，HEADERS 之后添加:
FORM_TYPE_MAP = {
    "annual":    "10-K",
    "quarterly": "10-Q",
}

# 将 list_10k 替换为 list_filings:
def list_filings(self, ticker: str,
                 form_types: Optional[list[str]] = None,
                 since_year: Optional[int] = None) -> list[dict]:
    """List SEC filings for a ticker.

    Args:
        ticker: Stock ticker symbol
        form_types: List of form types, e.g. ["10-K", "10-Q"]. Default to both.
        since_year: Filter filings with report_date earlier than this year.
    """
    if form_types is None:
        form_types = ["10-K", "10-Q"]

    cik = self._get_cik(ticker)
    if not cik:
        return []

    try:
        url = f"{self.BASE}/CIK{cik}.json"
        r = requests.get(url, headers=self.HEADERS, timeout=self.TIMEOUT)
        r.raise_for_status()
        data = r.json()

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        dates = filings.get("filingDate", [])
        reports = filings.get("reportDate", [])
        docs = filings.get("primaryDocument", [])
        accessions = filings.get("accessionNumber", [])
        descriptions = filings.get("primaryDocDescription", [])

        results = []
        for i in range(len(forms)):
            if forms[i] not in form_types:
                continue

            report_date = reports[i] or ""
            year = int(report_date[:4]) if report_date and len(report_date) >= 4 else 0
            if since_year is not None and year < since_year:
                continue

            acc = accessions[i].replace("-", "")
            # Map form type to report_type key
            rtype = "annual" if forms[i] == "10-K" else "quarterly"

            results.append({
                "form": forms[i],
                "report_type": rtype,
                "filing_date": dates[i],
                "report_date": report_date,
                "accession": accessions[i],
                "description": descriptions[i] if i < len(descriptions) else "",
                "url": f"{self.ARCHIVE}/{cik.lstrip('0')}/{acc}/{docs[i]}",
            })
        return results
    except Exception:
        return []
```

- [ ] **Step 2: 重构 `download_10k` → `download_filings`**

```python
# 将 download_10k 替换为 download_filings:
def download_filings(self, ticker: str, data_dir: str = "data/stock",
                     since_year: Optional[int] = None,
                     form_types: Optional[list[str]] = None) -> list[dict]:
    """Download SEC filings (10-K and/or 10-Q) for a ticker.

    Saves to data_dir/us/{ticker}/reports/{filename}.txt
    """
    results = self.list_filings(ticker, form_types=form_types, since_year=since_year)
    ticker_dir = Path(data_dir) / "us" / ticker / "reports"
    ticker_dir.mkdir(parents=True, exist_ok=True)

    for r in results:
        form = r["form"]
        year = r["report_date"][:4] if r["report_date"] else "unknown"
        filename = f"{year}_{form}_{ticker}.txt"
        dest = ticker_dir / filename
        if dest.exists() and dest.stat().st_size > 0:
            r["downloaded"] = True
            r["path"] = str(dest)
            continue

        try:
            resp = requests.get(r["url"], headers=self.HEADERS, timeout=60, stream=True)
            resp.raise_for_status()
            dest.write_text(resp.text[:500000], encoding="utf-8")
            r["downloaded"] = True
            r["path"] = str(dest)
            time.sleep(0.2)
        except Exception:
            r["downloaded"] = False

    return results
```

- [ ] **Step 3: 验证语法并提交**

```bash
python -m py_compile src/data/providers/edgar.py
```

```bash
git add src/data/providers/edgar.py
git commit -m "feat: EDGAR 从 10-K 专用重构为通用 filings 接口，支持 10-Q"
```

---

### Task 2: Gateway — US 分支透传新参数

**Files:**
- Modify: `src/data/gateway.py`

- [ ] **Step 1: 更新 `get_reports` US 分支**

将第 321 行 `self._edgar.download_10k(symbol)` 替换为：

```python
        # Map report_types to SEC form types
        if report_types is None:
            form_types = ["10-K", "10-Q"]
        else:
            form_types = [self._edgar.FORM_TYPE_MAP.get(rt, "10-K") for rt in report_types]

        return self._edgar.download_filings(
            symbol, since_year=since_year, form_types=form_types
        )
```

- [ ] **Step 2: 验证语法并提交**

```bash
python -m py_compile src/data/gateway.py
```

```bash
git add src/data/gateway.py
git commit -m "feat: Gateway US 报告分支透传 since_year/form_types 到 EDGAR"
```

---

### Task 3: CLI `fetch reports` — 去 A 股限制，加 US 映射

**Files:**
- Modify: `src/cli/commands/reports.py`

- [ ] **Step 1: 重写 `reports` 命令支持 US 市场**

```python
import click
from datetime import date
from rich.console import Console
from rich.table import Table

from src.data.gateway import DataGateway
from src.utils.ticker import parse_ticker, stock_dir

console = Console()

REPORT_TYPE_HELP = {
    "annual":       "年报",
    "semi_annual":  "半年报",
    "quarterly":    "季报",
}

US_REPORT_TYPE_HELP = {
    "annual":       "10-K 年报",
    "quarterly":    "10-Q 季报",
}


@click.command()
@click.argument("ticker")
@click.option("--type", "report_type", default="all",
              type=click.Choice(["all", "annual", "semi_annual", "quarterly"]),
              help="报告类型，默认 all")
@click.option("--years", default="", help="过滤年份，逗号分隔 (如 2024,2025)，默认近2年")
def reports(ticker: str, report_type: str, years: str):
    """报告下载 — PDF 原文 / SEC 文本 + Markdown 转换.

    A股: CNINFO (年报/半年报/季报)。
    美股: SEC EDGAR (10-K 年报 / 10-Q 季报)。
    """
    symbol, market = parse_ticker(ticker)

    # Parse years or default to last 2 years
    if years:
        parsed = [int(y.strip()) for y in years.split(",") if y.strip()]
        since_year = min(parsed) if parsed else date.today().year - 1
    else:
        since_year = date.today().year - 1

    # Parse report types
    if report_type == "all":
        report_types = ["annual", "semi_annual", "quarterly"] if market.value == "cn" else ["annual", "quarterly"]
    else:
        report_types = [report_type]

    gw = DataGateway()
    type_label = "全部" if report_type == "all" else REPORT_TYPE_HELP.get(report_type, report_type)
    scope = f"({years})" if years else "(近2年)"
    mkt_label = "A股" if market.value == "cn" else "美股"
    console.print(f"[bold]Fetching {mkt_label} {type_label} for {symbol} {scope}...[/bold]")

    results = gw.get_reports(symbol, market, since_year=since_year, report_types=report_types)

    if not results:
        console.print("[yellow]未找到报告[/yellow]")
        return

    # Choose label map by market
    label_map = US_REPORT_TYPE_HELP if market.value != "cn" else REPORT_TYPE_HELP

    table = Table(title=f"{symbol} 报告")
    table.add_column("年份")
    table.add_column("类型")
    table.add_column("标题")
    table.add_column("发布日期")
    table.add_column("文件")

    for r in results:
        file_ok = r.get("downloaded")
        file_icon = "[green]✓[/green]" if file_ok else "[red]✗[/red]"
        kind_label = label_map.get(r.get("report_type", ""), r.get("form", ""))
        title = r.get("title", r.get("description", ""))[:50]
        table.add_row(str(r.get("year", "")), kind_label, title,
                      r.get("publish_date", r.get("filing_date", "")), file_icon)

    console.print(table)

    if market.value == "cn":
        md_count = sum(1 for r in results if r.get("md_path"))
        if md_count > 0:
            console.print(f"\n[dim]PDF + Markdown 存储于 data/stock/cn/{stock_dir(symbol)}/reports/[/dim]")
    else:
        console.print(f"\n[dim]文本存储于 data/stock/us/{symbol}/reports/[/dim]")
```

关键改动：
- 去掉 `if market.value != "cn": console.print("[red]报告下载仅支持A股[/red]"); return`
- `gw.get_reports()` 增加 `market` 参数传递
- US 市场用 `US_REPORT_TYPE_HELP` 标签映射
- EDGAR 返回的字段名不同（`description` 而非 `title`, `filing_date` 而非 `publish_date`）

- [ ] **Step 2: 验证语法并提交**

```bash
python -m py_compile src/cli/commands/reports.py
```

```bash
git add src/cli/commands/reports.py
git commit -m "feat: fetch reports 支持美股 SEC EDGAR 10-K/10-Q"
```

---

### Task 4: 报告文本分析 — 加 US 路径查找

**Files:**
- Modify: `src/analysis/report_text.py`

- [ ] **Step 1: 更新 `_find_latest_report` 支持 US + CN 双市场**

```python
def _find_latest_report(ticker_dir: str, market: str = "cn") -> Optional[Path]:
    """Find the most recent report MD or TXT file.

    Args:
        ticker_dir: Stock directory name (CN) or ticker symbol (US)
        market: "cn" or "us"
    """
    if market == "us":
        reports_dir = Path(f"data/stock/us/{ticker_dir}/reports")
        if not reports_dir.exists():
            return None
        files = sorted(reports_dir.glob("*.txt"))
        return files[-1] if files else None
    else:
        reports_dir = Path(f"data/stock/cn/{ticker_dir}/reports")
        if not reports_dir.exists():
            return None
        files = sorted(reports_dir.glob("*.md"))
        # Prefer non-summary, non-补充
        full = [f for f in files if "摘要" not in f.name and "补充" not in f.name]
        return full[-1] if full else (files[-1] if files else None)
```

- [ ] **Step 2: 更新 `ReportTextAnalyzer.analyze` 支持 US 市场**

在 `analyze` 方法中，根据 `market` 查找报告：

```python
def analyze(self, context: AnalysisContext) -> AnalysisResult:
    ticker_str = context.ticker.symbol if hasattr(context.ticker, 'symbol') else str(context.ticker)
    market = context.ticker.market.value if hasattr(context.ticker, 'market') else "cn"
    
    report_path = _find_latest_report(ticker_str, market=market)
    # ... 后续分析逻辑不变
```

- [ ] **Step 3: 验证语法并提交**

```bash
python -m py_compile src/analysis/report_text.py
```

```bash
git add src/analysis/report_text.py
git commit -m "feat: 报告文本分析支持美股 SEC EDGAR 文本"
```

---

### Task 5: 端到端验证

- [ ] **Step 1: 测试美股报告抓取**

```bash
python -m src.cli.main fetch reports AAPL.US --type all
# 预期: 列出 10-K + 10-Q，下载文本
```

- [ ] **Step 2: 测试 --type 过滤**

```bash
python -m src.cli.main fetch reports AAPL.US --type quarterly
# 预期: 仅 10-Q
```

- [ ] **Step 3: 测试 --years 默认近 2 年**

```bash
python -m src.cli.main fetch reports AAPL.US
# 预期: 只下载报告期 >= 去年 的 filings
```

- [ ] **Step 4: 测试 CLI help**

```bash
python -m src.cli.main fetch reports --help
# 预期: 描述含"美股: SEC EDGAR"
```
